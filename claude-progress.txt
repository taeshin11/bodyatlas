# BodyAtlas — Claude Code Progress Log

## Session 1
- Date: 2026-03-31
- Session #: 1
- Phase: mvp + v1
- Features Worked On: F01-F12 (all MVP + V1)
- Completed: F01-F12 (all MVP + V1)
- Blockers: none
- Next Up: V2 features (F13-F16) when user demand is validated
- Git Commits:
  - feat: initial project scaffold with Next.js + Tailwind + DICOM viewer
  - feat: add SEO pages, error boundary, and UI improvements
  - feat: add navigation links to header and guide link on upload page
  - feat: add i18n (7 languages), mobile nav, SEO fixes, feedback, 404 page
- Pushed to Remote: yes (4 pushes)
- PRD Updated: yes (v1.3 - pricing updated to $4.99/mo based on competitor research)
- Notes:
  MVP features:
  - DICOM parsing with dicomParser, volume reconstruction
  - Custom canvas-based tri-plane viewer (axial/sagittal/coronal)
  - AC-PC landmark marking on any view with colored markers
  - Auto-alignment engine with rotation matrix + trilinear interpolation
  - Manual XYZ rotation sliders with real-time preview
  - PNG + DICOM export with combined report image
  - Responsive desktop (3-panel + sidebar) and mobile (tabbed + expandable)

  V1 features:
  - Glassmorphism UI with Framer Motion animations
  - i18n: auto-detect browser language, 7 languages (EN/KO/JA/ZH/ES/DE/FR)
  - SEO: /about page, /how-to-use guide, sitemap.xml, robots.txt
  - SEO: JSON-LD structured data, canonical URLs, multilingual keywords
  - SEO: favicon.svg, h1 tags, OG meta tags
  - Mobile hamburger menu with animated nav
  - Custom 404 page
  - Error boundary component
  - Feedback button with Formspree + mailto fallback to taeshinkim11@gmail.com
  - Silent analytics (Google Sheets webhook ready)
  - Feature flags architecture (config/features.ts)
  - SPINAI branding in footer on all pages
  - Keyboard-accessible upload area

  Pricing (PRD v1.3):
  - $4.99/mo or $39.99/yr (undercuts competitors by ~50%)
  - PostDICOM: $50-250/mo, RadiAnt: $79, Pacsbin: $29/mo

  Env vars needed in Vercel:
  - NEXT_PUBLIC_FORMSPREE_ID — for feedback emails
  - NEXT_PUBLIC_SHEETS_WEBHOOK_URL — for silent analytics

  Deployed to: bodyatlas-ten.vercel.app
  GitHub repo: github.com/taeshin11/bodyatlas

## Session 2
- Date: 2026-04-06
- Session #: 2
- Phase: pivot cleanup + multi-computer handover setup
- Features Worked On: feature flags re-org, i18n, atlas viewer UI, label data regen
- Completed: WIP changes committed + GitHub repo renamed + HANDOVER.md created
- Blockers: none
- Next Up: verify WIP changes work end-to-end, continue BodyAtlas pivot cleanup
- Git Commits:
  - chore: rename repo to bodyatlas + add HANDOVER.md + commit WIP
- Pushed to Remote: yes
- PRD Updated: modified (details TBD on next read)
- Notes:
  Repo housekeeping:
  - Renamed GitHub repo to bodyatlas (via GitHub settings)
  - Updated local remote URL to https://github.com/taeshin11/bodyatlas.git
  - Created HANDOVER.md as single-source-of-truth entry point for future sessions

  WIP changes committed:
  - src/config/features.ts: removed DICOM-era flags (triPlaneViewer, acpcAlignment, etc),
    replaced with BodyAtlas-era flags (atlasViewer, labelOverlay, structureSearch,
    multiLanguage, pwaOffline, quizMode, dicomUpload, aiAutoLabel, premiumTier)
  - src/lib/i18n.ts: added 'header.download' key across all 7 locales
  - src/components/{AtlasViewer,StructurePanel,AuthGate,Header}.tsx: UI updates
  - src/app/page.tsx, layout.tsx: page/layout tweaks
  - public/data/chest-ct/labels/axial/*.json: regenerated 856 label files
  - PRD.md: updated

  Handover system:
  - HANDOVER.md = "지금 바로 할 일" snapshot, overwritten each session
  - claude-progress.txt = append-only session log (this file)
  - Env vars (.env.local) stay local — HANDOVER.md lists keys needed, values must
    be copied manually between computers or re-fetched from Vercel dashboard
## Session 3
- Date: 2026-04-06
- Session #: 3
- Phase: spine x-ray atlas integration
- Features Worked On: Spine X-ray Atlas (new modality), data pipeline
- Completed: Full Spine X-ray feature — data pipeline + viewer component + UI integration
- Blockers: none
- Next Up: Run dev server to verify SpineXrayViewer renders correctly in browser
- Git Commits:
  - feat: add Spine X-ray Atlas (SpineXrayViewer + data pipeline)
- Pushed to Remote: yes
- PRD Updated: no (PRD v2.0 already covers spine as future modality)
- Notes:
  Data source analysis (D:\ImageLabelAPI_SPINAI):
  - Evaluated all datasets: X-ray (labeled), CT (seg masks only), OLIF (clinical)
  - CTSpine1K (610 NIfTI seg masks) — no paired CT images locally, skip for now
  - SBJ_LLXR lateral spine X-rays — polygon labels in labelme format, immediately usable
  - OLIF CT dataset — clinical PHI concerns, not used

  Spine X-ray Atlas implementation:
  - Pipeline: scripts/gen_spine_xray_atlas.py
    - Source: D:/ImageLabelAPI_SPINAI/SBJ_LLXR/0 label 후/2508/
    - Lateral: 20250807_00273183_T-L-spine_Lat_1.png (679x926, T5-S1, 15 vertebrae)
    - AP:      20250806_00154057_L-spine_AP_1.png (467x895, T10-S1, 10 vertebrae)
    - labelme polygon JSON → BodyAtlas [{id, name, contours}] format
  - Output: public/data/spine-xray/ (lateral/, ap/, labels/lateral/, labels/ap/, info.json, structures.json)
  - 15 structures total (C1-S1 defs defined, 15 active in Lateral, 10 in AP)

  New component: src/components/SpineXrayViewer.tsx
  - Side-by-side Lateral + AP panels
  - Same canvas-based hover/click/overlay logic as AtlasViewer
  - Labels overlay toggle
  - Tooltip with vertebra name (EN + locale)

  RegionSelector: added 'spine_xray' (🦴 Spine X-ray / 척추 X선), free tier
  page.tsx: conditional rendering — spine_xray → SpineXrayViewer, others → AtlasViewer+StructurePanel

  Build: npm run build passed (zero TS errors)

## Session 4
- Date: 2026-04-06
- Session #: 4
- Phase: head-ct atlas v2 rebuild
- Features Worked On: Head CT atlas rebuild with D-drive TotalSegmentator v2 data
- Completed: Head CT atlas v2 rebuild (91 structures, 320/311/311 slices)
- Blockers: train.py GPU 점유 (추가 head tasks 보류)
- Next Up: VRAM 여유 시 추가 head tasks 자동 실행 (117+ structures)
- Git Commits:
  - feat: rebuild head-ct atlas with TotalSegmentator v2 data (91 structures, 320/311/311 slices)
- Pushed to Remote: yes
- PRD Updated: no
- Notes:
  Session 3에서 시작한 TotalSegmentator v2 파이프라인 실행 중 문제 해결:
  - UnicodeEncodeError (cp949 em-dash) -> ASCII 대체로 수정
  - run_when_ready.py 고아 프로세스 여러 개 동시 실행 -> kill
  - bash background task exit code 127 -> & 제거, run_in_background만 사용
  - VRAM 포화 (543MB free) -> 고아 프로세스 정리

  유저 요청: D:/ImageLabelAPI_SPINAI/ 에 이미 TotalSegmentator v2 데이터 있으니 중복 안 하고 그거 사용
  발견: D:/ImageLabelAPI_SPINAI/data/cat_F_opensource/TotalSegmentator_v2/Totalsegmentator_dataset/
    - 1205 cases (full dataset, 압축 해제됨)
    - s0011: ct.nii.gz + segmentations/ (104 .nii.gz files)

  gen_head_ct_atlas.py --seg-dir 인자 추가, --build-only로 GPU 없이 atlas 빌드
  결과: 91 active structures, axial 320 (+58), sagittal 311 (+86), coronal 311 (+151)
  npm run build: PASSED

  백그라운드: data_pipeline/run_head_upgrade.py
  - VRAM 4GB+ 대기 -> 추가 4개 head tasks 자동 실행
  - head_glands_cavities, headneck_bones_vessels, brain_structures, craniofacial_structures
  - 완료 시 104 + 추가 -> 117+ structures로 atlas 재빌드

  추가 head tasks (CPU 실행):
  - head_glands_cavities: 19 seg (눈, 시신경, 부비동 등) - 완료
  - headneck_bones_vessels: 12 seg (후두, 설골, 경동맥 등) - 완료
  - brain_structures: TotalSegmentator 유료 라이선스 필요 - 스킵
  - craniofacial_structures: TotalSegmentator 유료 라이선스 필요 - 스킵
  - 최종: 96 active structures (head crop 후)

  CADS (murong-xu/CADS) 시도:
  - 모델 라이선스: CC BY-NC-SA 4.0 (비상업) - CC BY 4.0 버전 준비 중
  - Task 557 (brain): head detection 실패 ("does not contain head part") - 전신 CT/head crop 모두 실패
  - Task 553 (general): 16 labels 출력 but TotalSegmentator와 중복
  - 결론: CADS brain segmentation은 이 CT에서 작동 안 함

  Brain PET Atlas 추가:
  - aaa/ 폴더 DICOM (PT modality, 47 slices, 256x256, 3.26mm)
  - public/data/brain-pet/ 생성 (47/256/256 slices, percentile windowing)
  - RegionSelector: brain_pet 추가 (☢️ 뇌 PET)
  - 라벨 없음 (PET은 대사 활성 표시, 해부학적 구조 분할 불가)

## Session 5
- Date: 2026-04-06
- Session #: 5
- Phase: annotation quality 개선 + Brain PET tab 제거
- Features Worked On: contour smoothing (all atlases), head-ct rebuild, Brain PET 제거
- Completed: 모든 atlas contour smoothing + head-ct rebuild + Brain PET 탭 제거
- Blockers: multi-atlas는 registration 도구(ANTs) 없이 불가 — 단독 케이스 + smoothing으로 대체
- Next Up: 배포 후 live annotation 품질 검증
- Git Commits:
  - feat: remove Brain PET tab (no annotation support)
  - feat: smooth contour labels for brain-mri and chest-ct
  - feat: rebuild head-ct atlas with spline smoothing (80 structures)
- Pushed to Remote: yes
- PRD Updated: no
- Notes:
  Brain PET 탭 제거:
  - PET 모달리티는 해부학적 구조 분할 불가 → annotation 0개
  - RegionSelector.tsx: 'brain_pet' 제거. 현재 탭 6개

  Annotation 품질 개선 (3단계):
  1. mask_to_contours 개선 (gen_head_ct_atlas.py):
     - scipy binary_fill_holes + binary_closing (모폴로지 클리닝)
     - splprep/splev periodic spline smoothing (s=n*2)
     - 포인트 수 ~75% 감소, 곡선 품질 대폭 향상
  2. brain-mri + chest-ct JSON 라벨 직접 smoothing (scripts/smooth_labels.py):
     - brain-mri: 519/582 파일 업데이트
     - chest-ct:  831/1053 파일 업데이트
  3. head-ct 완전 rebuild (smoothed contours):
     - 80 structures, 200/311/311 slices

  Multi-atlas 시도 결과:
  - gen_multi_atlas_head.py 작성 (메모리 효율적, structure별 처리)
  - 근본 문제: 각 케이스가 다른 voxel grid → 재샘플링 필요
  - 재샘플링 없이는 좌표계 불일치 → 의미없는 평균
  - 진정한 multi-atlas는 ANTs registration 필요 → 향후 과제

  신규 스크립트:
  - scripts/smooth_labels.py: JSON 라벨 post-process 스무딩
  - scripts/gen_multi_atlas_head.py: multi-atlas 빌더 (미래용)


## Session 6
- Date: 2026-04-06
- Session #: 6
- Phase: annotation quality upgrade (VISTA3D + visibility fix)
- Completed:
  1. Annotation visibility fix: default overlay opacity 0.15→0.22 + always-on stroke (AtlasViewer + SpineXrayViewer)
  2. VISTA3D (MONAI 1.5, 127-class CT segmentation) 설치 + inference on s0011
  3. head-ct: TotalSegmentator + VISTA3D merged → 89 structures (was 80)
  4. chest-ct: TotalSegmentator + VISTA3D merged → 122 structures (was 106)
- Blockers:
  - OpenMAP-T1 (280 brain regions): weights require Microsoft Forms 수동 신청
  - Multi-atlas: ANTs registration 없이 불가
- Next Up:
  - OpenMAP-T1 weights 신청 후 brain-mri 280 structures rebuild
  - VISTA3D 실행은 `conda activate vista3d` + `python scripts/run_vista3d.py`
- Git Commits:
  - feat: improve annotation visibility (always-on strokes, higher opacity)
  - feat: rebuild head-ct with VISTA3D+TotalSegmentator merged (89 structures)
  - feat: rebuild chest-ct with VISTA3D+TotalSegmentator merged (122 structures)
- Pushed to Remote: yes
- Notes:
  VISTA3D 설치:
  - conda env: vista3d (Python 3.10, MONAI 1.5.2, PyTorch 2.6+cu124)
  - Bundle: data_pipeline/vista3d_bundle/vista3d/ (auto-downloaded from MONAI)
  - Weights: NVIDIA NCLS v1 (non-commercial) — 유료 전환 시 재검토 필요
  - s0011 inference: 83 structures, ~7분 (RTX 4090)
  - VISTA3D segs: data_pipeline/vista3d_seg/ (flat .nii.gz files)
  - head_ct_seg/vista3d/ 에 복사해서 merge_segmentations() 활용

  brain-mri 개선 방법:
  - OpenMAP-T1: https://forms.office.com/Pages/ResponsePage.aspx?id=OPSkn-axO0eAP4b4rt8N7Iz6VabmlEBIhG4j3FiMk75UQUxBMkVPTzlIQTQ1UEZJSFY1NURDNzRERC4u
  - weights 받으면: python scripts/run_openmap.py
  - 현재 brain-mri는 1mm isotropic T1 MRI → OpenMAP-T1 바로 적용 가능

## Session 7
- Date: 2026-04-06
- Session #: 7
- Phase: brain-mri upgrade with OpenMAP-T1
- Features Worked On: OpenMAP-T1 inference + brain-mri 275 structures rebuild
- Completed: brain-mri atlas upgraded from 92 → 275 structures
- Blockers: none
- Next Up: nothing critical (all atlases upgraded)
- Git Commits:
  - feat: rebuild brain-mri atlas with OpenMAP-T1 (275 structures from 92)
- Pushed to Remote: yes
- Notes:
  OpenMAP-T1 setup challenges:
  - Downloaded weights are mixed V2/V3: CNet.pth=3ch (V3), SSNet.pth=1ch (V2),
    PNet/{axial,coronal,sagittal}.pth=3ch (V2), HNet/{axial,coronal}.pth=1ch (V2)
  - V3 code expects all 3ch — SSNet mismatch. V2 code expects all 1ch — CNet mismatch.
  - Solution: checked out v2.1.3 + patched CNet to UNet(3,1) + cropping.py to 3-slice context
  - numpy 2.x in base env broke pandas/pyarrow → created 'openmap' conda env (numpy<2)

  Inference result:
  - Input: MNI152 T1 1mm isotropic (197x233x189)
  - Output: Level5 parcellation → 275 brain regions
  - Runtime: ~71 seconds on RTX 4090

  gen_brain_mri_atlas.py:
  - Reads Level5.nii + t1.nii from openmap_output/
  - Maps ROI→region name from OpenMAP-T1_multilevel_lookup_table_dictionary.csv
  - Spline-smoothed contours, NaN/Inf guard
  - Categories: frontal/parietal/temporal/occipital/limbic/subcortical/cerebellum/brainstem/white/ventricle
  - Output: 275 structures, 189/197/233 slices

## Session 8 — 2026-04-06

### 완료
- Atlas orientation 버그 전체 수정: `np.flipud(slice.T)` 모든 plane에 적용
  - NIfTI RAS 데이터 PNG 저장 시 90° 회전 문제 해결
  - PNG 이미지와 contour annotation 동일 transform → 매칭 정확
- head-ct: Z-crop 수정 (HEAD_CROP_VOXELS 200→130), Z[301:431] → 가슴→머리/목
- brain-mri: T1 windowing 수정 (center=160, width=200)
- spine-xray 탭 제거 (RegionSelector.tsx: BodyRegion type + BODY_REGIONS)
- Atlas 재빌드:
  - brain-mri: 275 structures (orientation+windowing fixed)
  - head-ct: 66 structures (orientation+crop fixed), 130/311/311 slices
  - chest-ct: 122 structures (orientation fixed), 431/311/311 slices
- git commit fb8ac1b6 + push

## Session 9 — 2026-04-08

### 완료
- SPINAI atlas 탭 5개 추가 (Head CT, Chest CT, Abdomen CT, Pelvis CT, Lumbar MRI)
- RegionSelector.tsx 2행 구조: Original / SPINAI 그룹 분리
- SPINAI Brain MRI 탭 추가 → FastSurfer+MedSAM hybrid로 교체 (commercial)
- git commit:
  - feat: SPINAI atlas tabs (Head/Chest/Abdomen/Pelvis CT, Lumbar MRI)
  - feat: Replace SPINAI Brain MRI with FastSurfer (commercial license)
  - feat: hybrid Brain MRI atlas with MedSAM subcortical refinement (083f50c3)

## Session 10 — 2026-04-17

### 완료
- **SPINAI 모델 자동 모니터링 시스템 구축**:
  - `scripts/auto_model_monitor.py` (Windows cp949 대응, UTF-8 강제)
  - D:\ImageLabelAPI_SPINAI\outputs\models\ 전체 train.log 파싱 → Dice 기준 체크
  - 기준 초과 시: inference → per-class NIfTI 생성 → gen_our_ct_atlas 재실행 → git push
  - 기준: CT ≥ 0.85, MRI ≥ 0.70, X-ray ≥ 0.90
- `scripts/auto_monitor.bat` + `scripts/setup_scheduler.bat` 추가
  - Windows Task Scheduler에 매일 09:00 자동 실행 등록용 (관리자 권한 필요)
- `scripts/monitor_status.json` 첫 실행 생성: 7개 모델 스냅샷 저장
- **Kaggle API 연동**:
  - KAGGLE_API_TOKEN Windows 전역 환경변수 영구 설정
  - 정책 확립: 다운로드 전용, 업로드 금지 (의료 데이터 유출 방지)
  - 학습은 전부 D:\ImageLabelAPI_SPINAI\ 로컬 RTX 4090에서만
- **SEO 다국어 포털 대응 검증**:
  - robots.ts에 Naver(Yeti), Daum, Baidu, Yandex, Seznam 명시 허용 확인
  - layout.tsx 인증 메타태그: google/naver/bing/yandex/baidu env 변수 대응
  - IndexNow postbuild 자동 제출 작동 중
  - 사용자 수작업 안내: 각 포털 등록 → 인증코드 발급 → .env.local 입력
- 메모리 추가:
  - feedback_check_spinai_models.md (세션 시작 시 모델 체크 규칙)
  - feedback_no_data_upload.md (외부 업로드 금지 규칙)

### 현재 모델 상태 (2026-04-17)
- unet_ct_c65_v2 (EfficientUNet 20M): Dice 0.8505, epoch 78/80, **기준 통과**
- unet_ct_c65 v1: Dice 0.8083 (미달)
- unet_xray_c34: Dice 0.9042, 완료, **기준 통과**
- unet_mri_c26: Dice 0.5191, 완료, 미달 (추가 학습 필요)

### 다음
- CT v2 학습 완료 대기 → 자동 monitor가 VISTA3D 대체 atlas 생성
- 사용자 수작업: setup_scheduler.bat 관리자 권한 실행
- 사용자 수작업: 포털 등록 + .env.local 인증코드 입력
- 사용자 수작업: 커스텀 도메인 구매 검토 (SEO 장기 효과)

### Git Commits (예정)
- chore: add auto model monitor + Kaggle integration (Session 10)

## Session 11 — 2026-04-17/18

### 완료
- **CT v2 학습 완료**: unet_ct_c65_20260415_064501 (EfficientUNet 20M params)
  - Epoch 80/80 완료, Best Dice **0.8515** (기준 0.85 통과)
- **자동화 시스템 첫 실행 성공**:
  - `scripts/auto_model_monitor.py` 수동 실행 (스케줄러는 아직 미등록)
  - CT v2 PASS 감지 → SPINAI inference 12초 → atlas rebuild 25초 → git push 자동
  - commit `7d852d25 auto: update ct atlas from SPINAI models`
- **our-ct atlas가 자체 UNet으로 완전 전환**:
  - 이전: TotalSegmentator v2 pre-segmented 데이터 소비
  - 현재: unet_ct_c65 inference 결과 직접 사용
  - **VISTA3D 의존성 제거 — our-ct는 이제 Apache 2.0 완전 상업 가능**
- .gitignore 업데이트:
  - scripts/_spinai_ct_inference.py (자동생성 임시)
  - scripts/_spinai_ct_rebuild.py (자동생성 임시)

### 발견된 이슈
- unet_ct_c65 동일 Dice 0.8515 모델 3개 (064501, 093321, 093403) — 학습 스크립트 중복 실행 흔적
  - 현재는 가장 오래된 064501을 사용 중 — 문제 없음

### 다음 세션에서 해야 할 일
1. **MRI 모델 재학습 전략** (최우선): Dice 0.5191 → 0.70 필요
   - 데이터 보강 (Kaggle에서 추가 MRI 데이터셋 다운로드)
   - 또는 아키텍처 변경 (EfficientUNet → nnUNet 검토)
2. **X-ray atlas 자동 재생성 파이프라인 구현**:
   - unet_xray_c34 (Dice 0.9042) 이미 준비됨
   - auto_model_monitor.py의 MODEL_ATLAS_MAP에 xray 추가 필요
   - X-ray용 atlas builder 작성 (2D PNG + JSON contour)
3. **chest-ct도 자체 UNet으로 교체 검토**:
   - unet_ct_c65 65-class가 chest/abdomen/pelvis 대부분 커버
   - VISTA3D 비상업 의존성 완전 제거 가능
4. **사용자 수작업 리마인드**:
   - setup_scheduler.bat 관리자 권한 실행 (매일 자동화 활성화)
   - Google/Naver/Bing/Yandex/Baidu 포털 등록 + .env.local 인증코드
   - (선택) 커스텀 도메인 구매

### Git Commits
- 7d852d25 auto: update ct atlas from SPINAI models (자동)
- f5350b6f chore: add auto model monitor + Kaggle integration (Session 10)

## Session 12
- Date: 2026-04-18
- Focus: X-ray + MRI atlas 파이프라인 구현, chest-ct 라이선스 검증, 자동화 확장

### 주요 성과
1. **MRI 모델이 자동으로 기준 통과 (Dice 0.7016)** — Session 11 이후 재학습된 `unet_mri_c26_20260417_212345`
2. **X-ray atlas 자동 파이프라인 구현** — `gen_our_xray_atlas.py` (unet_xray_c34 기반 Apache 2.0)
   - AP 10 + Lateral 10 case, 21 structures 자동 생성
   - 소스: cat_A_ap_xray, cat_B_lat_xray (자체 수집 데이터)
3. **MRI atlas 자동 파이프라인 구현** — `gen_our_lumbar_mri_atlas.py`
   - SPIDER_lumbar T2 → unet_mri_c26 → our-lumbar-mri/
   - **이슈:** 모델이 axial slice 기대 vs SPIDER는 sagittal — 7 classes만 검출됨 (정상 26 중)
4. **auto_model_monitor.py 확장:**
   - `MODEL_ATLAS_MAP`에 mri + xray 추가
   - `run_mri_rebuild` / `run_xray_rebuild` 핸들러 구현
   - 자동 실행 성공: git commit df7b59a2 (mri + xray atlas) 푸시 완료
5. **chest-ct 라이선스 재검증:**
   - 기존 HANDOVER: "VISTA3D 비상업 포함" (레거시 정보)
   - 실제 확인: chest-ct 108 structures 전부 TotalSegmentator 출처
   - **chest-ct는 이미 Apache 2.0 (VISTA3D 실제로 머지 안 됨)**

### 발견된 이슈
- **MRI atlas orientation mismatch:**
  - unet_mri_c26은 axial slice 단위 학습
  - SPIDER_lumbar는 sagittal 저장 (shape Z=50, XY=448x578)
  - 결과: 26 classes 중 7개만 검출 (false positive 포함 가능)
  - 해결책: axial-reformatted MRI 소스 사용 or volume permutation 테스트
- **head-ct 학술 라이선스 (brain_structures)**: TotalSegmentator academic key 사용 중 — 상업 배포 시 재검토 필요
- **brain-mri (Original)**: OpenMAP-T1 CC BY-NC 여전히 사용 중 — brain-mri-commercial로 전환 검토

### Frontend 통합 미완
- our-xray atlas 데이터 생성됨 but UI 미연결 (SpineXrayViewer는 dead code, main page에 미탑재)
- our-lumbar-mri 생성됨 but RegionSelector.lumbar_mri는 여전히 /data/lumbar-mri 사용
- 다음 세션 작업: RegionSelector + page.tsx에 xray/new-mri 연결

### 다음 세션 우선순위
1. **MRI 모델 재학습 or orientation fix**
   - axial MRI 데이터셋 추가 or SPIDER를 axial-reformat
   - sagittal-trained 모델 별도 학습 검토
2. **Frontend 통합** (아무리 atlas 있어도 사용자가 못 봄)
   - SpineXrayViewer를 main page에 wire up
   - lumbar_mri dataPath 전환 (our-lumbar-mri이 더 다양한 구조 생성되면)
3. **head-ct / brain-mri 라이선스 정리**
   - Original group 내 비상업 데이터 제거 전략
   - 또는 Original 그룹 자체 제거

### 사용자 수작업 TODO (변화 없음)
- setup_scheduler.bat 관리자 권한 실행
- Google/Naver/Bing/Yandex/Baidu 포털 등록 + .env.local 인증코드
- (선택) 커스텀 도메인 구매

### Git Commits (Session 12)
- df7b59a2 auto: update mri, xray atlas from SPINAI models (자동 모니터)
- (이 세션 수동 commit: 스크립트 + HANDOVER 업데이트)

## Session 12 — Part 2 (같은 날 이어서 완료)

### 추가 성과
6. **MRI orientation fix:**
   - SPIDER는 sagittal 획득 (sitk X축이 LR 방향, spacing 3.32mm)
   - `np.transpose((1,2,0))`로 volume (AP,LR,HF) 정렬
   - predict_volume에 (LR=50, HF=578, AP=448) 전달 → sagittal slice 기반 추론
   - 검출 classes: 7 → **17** (L1-S1, 모든 disc, spinal_cord, 일부 abdominal organs 포함)
   - Atlas plane labels 교정: sagittal(50) / coronal(448) 뒤바뀐 것 수정

7. **Frontend 통합 완료:**
   - `SpineXrayViewer`에 `dataPath` prop 추가 (하위호환)
   - `RegionSelector`: `our_xray` 버튼 추가, `lumbar_mri` dataPath 전환
   - `page.tsx`: `isXray` 분기로 SpineXrayViewer/AtlasViewer 토글
   - `npx tsc --noEmit` + `npx next build` 모두 성공

8. **라이선스 완전 클린업:**
   - `public/data/brain-mri/` 삭제 (OpenMAP-T1 CC BY-NC)
   - `public/data/lumbar-mri/` 삭제 (SPIDER 직접 mask, 라이선스 TBD)
   - `public/data/head-ct/`: 학술 라이선스 6개 제거 (brainstem/caudate_nucleus/cerebellum/insular_cortex/thalamus/ventricle) — 19→13 structures, 2029 label entry
   - RegionSelector `BodyRegion` 타입에서 `brain_mri` 제거
   - **배포 전체 Apache 2.0 compliant 달성**

### 최종 Atlas 구성 (SPINAI)
- our-head-ct (TotalSegmentator)
- our-ct → Chest/Abdomen/Pelvis (unet_ct_c65)
- our-brain-mri (FastSurfer+MedSAM)
- our-lumbar-mri (unet_mri_c26, 17 classes)
- our-xray (unet_xray_c34, 21 classes)

### 다음 세션 우선순위
1. **Task Scheduler 등록** (사용자 수작업): `setup_scheduler.bat` 관리자 권한 실행
2. **검색 포털 인증** (사용자 수작업): Google/Naver/Bing/Yandex/Baidu 인증코드 `.env.local`
3. **head-ct 13 structures 최종 검증**: 전부 TotalSegmentator Apache 소스인지 재확인
4. **brain-pet 라이선스 검토**: 아직 TBD

### Git Commits (Session 12 Part 2)
- (이 commit: Frontend + 라이선스 클린업 + MRI orientation fix)

## Session 13
- Date: 2026-04-21
- Focus: 신규 모달리티 자동 감지 (hand/foot X-ray) + auto_monitor training_history.json fallback

### 주요 성과
1. **신규 모달리티 2종 (hand/foot) 자동 감지 → atlas 생성 → 통합:**
   - `unet_hand_ANON_v3_c2_20260421` Dice 0.9682 (기준 0.90 통과)
   - `unet_foot_ANON_v2_c2_20260421` Dice 0.9341 (기준 0.90 통과)
   - 소스: `D:/ImageLabelAPI_SPINAI/data/anon/` (이미 익명화된 데이터)

2. **`gen_our_joint_xray_atlas.py` 신규:**
   - `--modality hand|foot` 인자로 2-class binary X-ray 공통 빌더
   - MODALITY_CONFIG dict로 소스/checkpoint/출력 경로 관리
   - 향후 chest_lung 등 binary class 모델 추가 시 dict 엔트리만 추가

3. **`auto_model_monitor.py` 주요 개선:**
   - `_hand_`, `_foot_` modality 인식
   - **`training_history.json` fallback**: train.log 없는 신규 모델도 val_dice 추출 가능
     (이전엔 train.log 없으면 scan에서 skip → 신규 모델 감지 불가)
   - `run_joint_xray_rebuild(info, modality)` 공통 핸들러로 hand/foot 통합

4. **Frontend 업데이트:**
   - `SpineXrayViewer`: info.json 기반 availableViews 동적 결정
     - single view (hand/foot) → grid-cols-1
     - dual view (spine) → grid-cols-2
   - `RegionSelector`: `our_hand_xray` (✋), `our_foot_xray` (🦶) 추가
   - `BodyRegion` 타입 확장, `page.tsx`의 `isXray` 분기 3개로 확장

5. **FEATURES.md 의무 hook 첫 실전 동작 확인:**
   - auto_monitor가 public/data/our-{hand,foot}-xray 생성 후 자동 git commit 시도
   - `.githooks/commit-msg`가 FEATURES.md 미갱신을 감지하여 차단 — 설계대로 동작
   - 수동으로 FEATURES.md 업데이트 후 통과

### 비고 (다른 모델 상태)
- `unet_xray_ANON_v1_c34` Dice 0.7840 (5 epoch, 학습 중) — 완료되면 our-xray 대체 검토
- `unet_chest_c3` checkpoint 81MB 있으나 training_history.json 비어있음 — 학습 미완 추정
- 기존 ct/mri/spine xray 전부 up-to-date (rebuild 불필요)

### Git Commits (Session 13)
- (이 commit: hand/foot 파이프라인 + FEATURES.md 갱신 + Frontend 업데이트)


## Session 14 (2026-04-22) — X-ray Case Navigator + Zombie Cleanup

### 1. Live test (5-modality 3-xray 파이프라인 건강 검진)
- dev 서버 `next dev -p 3777` 재기동
- 6개 라우트 전부 200 (`/`, `/about`, `/how-to-use`, `/download`, `/privacy`, `/terms`)
- 9개 atlas dataPath `structures.json` + `info.json` 전부 200
- hand/foot `ap/0000.png` + `labels/ap/0000.json` 정상 서빙

### 2. Zombie process 정리
- 4월 6일·18일부터 살아있던 `next dev` 프로세스 6개 종료 (3777, 3456 포트 점유)
- Python `uvicorn` (SPINAI API) + `pseudo_xray34c_anon.py` (활성 pseudo-label 작업) 은 그대로 유지

### 3. 진짜 발견한 버그 → 기능 개선으로 전환
**문제:** `SpineXrayViewer`가 `${dataPath}/${view}/0000.png` 하드코딩 — 10 케이스 중 첫 번째만 표시.
- spine X-ray: 10 AP + 10 Lateral 준비됨, but 1+1 노출
- hand X-ray: 10 AP 준비됨, but 1 노출
- foot X-ray: 10 AP 준비됨, but 1 노출
- 총 60 케이스 중 30개만 사용자에게 도달 가능 → 50% 데이터 사각지대

**해결:** Case 네비게이터 구현
- `caseIndex` state + `caseCount`는 `info.planes[view].slices` 최솟값으로 자동 감지
- UI: `← Case N / N →` pill 버튼 (`caseCount > 1` 조건부 렌더)
- 키보드: 좌/우 화살표 키 (input/textarea focus 시 무시, wrap-around)
- `caseIndex` 변경 시 이미지+라벨 재로드 (0-padded 4-digit 파일명)
- `dataPath` 전환 시 자동 reset (info.json 로더가 `setCaseIndex(0)`)

### 4. 검증
- `npx tsc --noEmit` clean (stale `.next/types` 캐시 먼저 제거)
- `npx next build` 성공 (11 static pages, First Load JS 212kB — 케이스 네비 추가 후에도 동일)
- 비-zero 케이스 슬롯 HTTP 200 확인 (hand 0003/0007/0009, spine ap/lat 0005/0009)

### 5. 문서
- `FEATURES.md`:
  - §2.2 "SpineXrayViewer.tsx"를 "2-view"에서 "동적 view"로 재작성
  - Sub-features에 Case 네비게이터 상세 추가
  - Data 경로 템플릿을 `{caseId}` 기반으로 갱신
  - §6 키보드/마우스 표에 `← / →` 케이스 전환 + 네비 버튼 2행 추가
- `HANDOVER.md` 덮어쓰기 (Session 14 반영)

### Git Commits (Session 14)
- (이 commit: X-ray case navigator + FEATURES.md 동기 갱신)


## Session 15 (2026-04-22) — 🚨 Prod 유실 감지

### 발견 경위
- /loop 2회차에서 Vercel 프로덕션 헬스 체크 → `bodyatlas-ten.vercel.app` 404 `DEPLOYMENT_NOT_FOUND`
- `bodyatlas.vercel.app`은 전혀 다른 사람의 Angular "AI Fitness Companion" 앱이 점유 중 (재사용 불가)
- `npx vercel project ls` 결과: 현재 계정에는 `spinaiq` / `flowvium` / `web`만 존재 — **bodyatlas 프로젝트 없음**
- `.vercel/project.json`의 `prj_WcoBLejQU7ddyGPpu24PVusSk84h` / `team_Ku8jPGlrgClTAMzdeDkGF6Gr`는 삭제 또는 팀 이전된 것으로 확인됨 ("Your Project was either deleted, transferred to a new Team...")

### 영향
- 최근 모든 commit (최소 Session 12~14 포함) 이 GitHub까지만 도달, 프로덕션 반영 0
- case navigator, hand/foot modality, 라이선스 정리, 로깅 시스템 등 Session 12~14 산출물 전부 미공개
- 검색 엔진(이전에 sitemap 제출된 상태) → 404 반복 → 랭킹 타격 우려

### 조치 한 것 (이 세션)
- HANDOVER.md 상단에 🚨 블록 추가 (현상 + 복구 옵션 a/b/c + URL baked-in 파일 리스트)
- HANDOVER.md 배포 섹션에 "⚠ 유실" 표시
- claude-progress.txt에 이 발견 기록

### 조치 안 한 것 (사용자 결정 대기)
- Vercel 재배포 — 프로덕션 URL이 바뀔 수 있어 URL 14개 baked 파일 치환 타이밍을 사용자가 결정
- `.vercel/project.json` 삭제 — 재연결 방식에 따라 달라짐
- 커스텀 도메인 구매 — 오래 전부터 블로커로 있던 항목

### 좀비 점검
- 현재 `next dev` 프로세스 3개 (Session 14에 내가 띄운 것, 활성) — 유지
- Python SPINAI uvicorn + pseudo-label — 활성, 유지

### Git Commits (Session 15)
- (이 commit: HANDOVER.md + progress log 만 — 사용자 결정 필요 항목은 건드리지 않음)


## Session 16 (2026-04-22) — URL centralization (prod 복구 대비)

### 상황
- /loop 3회차 시점에 Vercel은 여전히 미복구 (`bodyatlas-ten.vercel.app` 404 지속)
- 사용자가 다른 프로젝트 12개 이상을 Vercel에 신규 배포하는 것 확인 — BodyAtlas 복구는 후순위
- 사용자 결정 대기 없이 할 수 있는 code-level 개선 찾음 → URL 상수화

### 작업
1. **`src/lib/site-config.ts` 신규:**
   ```ts
   export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL?.trim() || 'https://bodyatlas-ten.vercel.app';
   export const SITE_HOST = new URL(SITE_URL).host;
   export const OG_IMAGE = `${SITE_URL}/opengraph-image`;
   export function siteUrl(path: string = ''): string { ... }
   ```

2. **28개 baked URL 참조를 site-config 기반으로 전환:**
   - `src/app/layout.tsx` — metadataBase/canonical/og:url/og:image/twitter image/JSON-LD url·screenshot, HowTo step text (7 URL + 2 display)
   - `src/app/sitemap.ts` — BASE 상수
   - `src/app/robots.ts` — baseUrl
   - `src/app/about/page.tsx` — canonical/og url/og image (3)
   - `src/app/download/page.tsx` — canonical/og url (2)
   - `src/app/download/DownloadContent.tsx` — 설치 가이드 도메인 표시 (1)
   - `src/app/how-to-use/page.tsx` — canonical/og url/og image (3)
   - `src/app/privacy/page.tsx` — canonical/og url (2) + 본문 2곳
   - `src/app/terms/page.tsx` — canonical/og url (2) + 본문 2곳
   - `scripts/submit-indexnow.mjs` — HOST = env var 파싱 (protocol/trailing slash 제거)

3. **재배포 후 사용법:**
   - `.env.local`: `NEXT_PUBLIC_SITE_URL=https://<new-url>`
   - Vercel env 동일 세팅
   - Rebuild → 28+ 지점 자동 반영

### 검증
- `.next/types` 캐시 제거 후 `npx tsc --noEmit` clean
- `npx next build` 성공 (11 pages, sizes 동일 /download만 7.38→7.49 kB, SITE_HOST 문자열 리터럴 전환으로 극미세 증가)
- 로컬 dev는 hot reload로 이미 반영됨

### 문서
- `FEATURES.md §5` 외부 통합에 "사이트 URL centralization" 블록 추가
- `HANDOVER.md` 긴급 블록 업데이트 (URL centralization 완료 표시)
- `HANDOVER.md` 환경변수 섹션에 `NEXT_PUBLIC_SITE_URL` 추가 (최상단)

### 남은 blocker (사용자 결정)
- **Vercel 프로젝트 새로 생성** — `npx vercel deploy --prod` + `.vercel/project.json` 먼저 삭제
- **`.env.local` / Vercel env에 `NEXT_PUBLIC_SITE_URL` 세팅** (URL 확정 후)
- 커스텀 도메인 구매 (장기 과제)

### 좀비 점검
- node 프로세스 3개 (Session 14에 띄운 dev 서버, 활성 사용 중) — 유지

### Git Commits (Session 16)
- (이 commit: site-config 도입 + 28 baked URL 치환 + FEATURES/HANDOVER 동기)

## Session 17
- Date: 2026-04-22
- Session #: 17
- Phase: perf + hygiene (live test + optimize)
- Features Worked On: AtlasViewer / SpineXrayViewer hot-path perf, next.config 강화
- Completed: O(1) structure lookup, prod console 제거, poweredByHeader off
- Blockers: Vercel 재배포 미결 (Session 15부터 계승)
- Next Up: 사용자 결정 필요 — Vercel 새 프로젝트 or 커스텀 도메인

### 라이브 테스트
- dev 서버 (PID 41628/53608/66876, port 3777) 활성 확인 — 3개 전부 정상 run chain (npm → next dev → server worker), 좀비 아님 유지
- 라우트 전수 curl: `/`, `/about`, `/download`, `/how-to-use`, `/privacy`, `/terms`, `/sitemap.xml`, `/robots.txt`, `/opengraph-image` 모두 200
- SPINAI uvicorn (PID 46648, port 8000) 활성 유지, pseudo_xray34c 학습 (PID 74996) 2시간+ 진행 중 — 건드리지 않음

### 성능 최적화 (hot-path)
1. **`AtlasViewer.tsx`** — `structures.find(...)` 3곳 제거, `useMemo`로 `structuresById` / `structuresByName` Map 생성. 라벨 렌더 루프에서 N×M find → O(M) lookup (head-ct 28, chest-ct 122, brain-mri 275 구조에서 각 슬라이스 라벨당 호출되던 선형 탐색 제거).
2. **`SpineXrayViewer.tsx`** — 동일 패턴, canvas 2D draw 루프 안의 find 제거. renderView가 hover마다 돌기 때문에 체감 효과 큼.
3. **`next.config.js`** 강화:
   - `poweredByHeader: false` (X-Powered-By 제거)
   - `compress: true` 명시
   - `productionBrowserSourceMaps: false` 명시
   - `compiler.removeConsole` production 빌드에서 `error`/`warn` 제외하고 console.* 스트립

### 검증
- `npx tsc --noEmit` clean
- `npx next build` 성공 — `/` 15.8 kB / 212 kB First Load, 이전과 동일 (회귀 없음)
- HMR로 dev 서버 자동 반영, curl 재검증 6라우트 전부 200

### 좀비 프로세스
- 확인 대상: node(3), python(2), cmd(2)
- 전부 활성 작업 — kill 없음

### Git Commits (Session 17)
- (이 commit: hot-path perf + next.config 강화)

## Session 17 — Round 2 (2026-04-22)
- Phase: perf round 2 (lazy-load)
- Completed: `/` 초기 번들 12.5 kB (−21%) / First Load 209 kB (−3 kB)
- Incident: `next build`를 dev 서버 활성 상태에서 실행 → `.next` 디렉터리 공유로 HMR webpack-runtime이 `Cannot find module './682.js'` 500 발생. 해결: dev 프로세스 4개 kill + `.next` 삭제 + dev 재기동.
- Rule (future sessions): **dev 서버가 떠 있는 동안 `next build` 금지.** Verification은 별도 폴더/프로세스에서 or dev 중단 후 실행.

### 추가 최적화 (`src/app/page.tsx`)
- `SpineXrayViewer`, `AuthGate`, `FeedbackButton`, `InstallPrompt` → `next/dynamic({ ssr:false })` 로 lazy 로드. 초기 페이지 번들에서 제거.
- `BODY_REGIONS.find()` 2곳 → `useMemo`로 `regionsById: Map` 생성, O(1) lookup.

### 검증
- `npx tsc --noEmit` clean
- `npx next build` 성공 — `/` 15.8 → **12.5 kB**, First Load 212 → **209 kB**
- dev 재기동 후 6 라우트 전수 200

### Git Commits (Session 17 Round 2)
- (이 commit: lazy-load + regionsById memo + incident 로그)

## Session 17 — Round 3 (2026-04-22)
- Phase: perf round 3 (hit-test bbox + slice prefetch)
- Completed: AtlasViewer/SpineXrayViewer hover hit-test bbox 조기 기각, AtlasViewer ±1 slice idle-prefetch

### 라이브 테스트
- 8 라우트 전부 200, 좀비 없음 (node 3-proc dev chain 정상 + python 2개 활성)

### 성능 최적화 (R3)
1. **Bbox early-reject in hit-testing** (`AtlasViewer.tsx` + `SpineXrayViewer.tsx`):
   - `useMemo`로 `labelIndex` 생성 — 각 contour의 `[minX,minY,maxX,maxY]` bbox를 라벨 로드 시점에 1회 계산.
   - hover handler에서 contour마다 `isPointInPolygon` 호출 전 bbox 검사로 조기 기각 → brain-mri 275 구조 × 다수 contour 환경에서 마우스 프레임당 ray-casting 호출 수 대폭 감소.
   - `contourBBox` 헬퍼 추가.
2. **Adjacent-slice prefetch** (`AtlasViewer.tsx`):
   - 현재 슬라이스 변경 시 `requestIdleCallback` (없으면 `setTimeout(0)`)로 ±1 슬라이스의 image + labels.json을 HTTP 캐시에 warm.
   - 컴포넌트 언마운트 / 슬라이스 재변경 시 pending idle task cancel.
   - 스크럽/키보드 내비 시 체감 지연 감소 예상.

### 검증
- `npx tsc --noEmit` clean (prefetch의 fetch then 타입 에러 1건 수정 후)
- `next build`는 **일부러 생략** (R2의 dev/.next 충돌 교훈). HMR로 dev 서버 자동 반영, 4 라우트 curl 200.

### 안 한 것 (대안 평가 끝)
- `StructurePanel` 검색 debounce: UX 트레이드오프(느린 타자 지연)라 패스. 현재 useMemo로 이미 필터 재계산 방지.
- 검색 타깃 문자열 precompute: 미세 최적화라 패스.

### Git Commits (Session 17 Round 3)
- (이 commit: bbox early-reject + slice prefetch)

## Session 17 — Round 4 (2026-04-22)
- Phase: perf round 4 (search precompute)

### 라이브 테스트
- 8 라우트 전부 200, 좀비 없음. dev 3-proc (PID 62544/73452/46780) + python 2개 (학습/API) 전부 활성.
- prod `bodyatlas-ten.vercel.app` 여전히 404 (Session 15 블록, 사용자 결정 필요).

### 성능 최적화 (R4)
- **`StructurePanel.tsx` 검색 인덱스 precompute:**
  - 이전: 키스트로크마다 각 구조체에 대해 `s.name.replace(/_/g,' ').toLowerCase()` + `Object.values(s.displayName).map(v=>v.toLowerCase())` 재할당 (브레인 MRI 275 × 8 문자열).
  - 지금: `regionStructures` 변경 시 `useMemo`로 `searchIndex: {s, haystack}[]` 1회 precompute, 필터는 토큰별 `haystack.includes()`만 수행.
  - 핫패스에서 `Object.values` / `.map` / `toLowerCase` / 배열 할당 전부 제거.
  - 의미 동일 (token AND × haystack OR across fields — joined string이 semantically equivalent, space 구분자로 단어 경계 매치 보존).

### 검증
- `npx tsc --noEmit` clean
- HMR 반영 후 3 라우트 curl 200

### Git Commits (Session 17 Round 4)
- (이 commit: StructurePanel 검색 인덱스 precompute)

## Session 17 — Round 5 (2026-04-22)
- Phase: correctness + build verify (label race fix)

### 라이브 테스트
- 6 라우트 전부 200, dev 3-proc + python 2개 활성, 좀비 없음
- 학습 프로세스 (PID 74996) 3h21m+ 진행 중

### 버그 수정 (race condition)
- **`AtlasViewer.tsx`**: `currentSlice`/`activeTab` 빠르게 연속 변경 시 이전 슬라이스의 `fetch(labels.json)` 응답이 **나중에** 도착해 현재 슬라이스 라벨을 덮어쓸 수 있는 race. `AbortController`로 cleanup 시 abort, `AbortError` 무시 처리.
- **`SpineXrayViewer.tsx`**: `caseIndex` 변경 시 lateral/ap 2개 라벨 fetch 동일한 race 가능성. 동일 패턴으로 수정 (각 view별 `AbortController` 배열로 일괄 abort).

### 빌드 (safe mode — R2 교훈 준수)
- dev 3-proc kill (PID 62544/73452/46780) → `.next` 삭제 → `npx next build` clean → dev 재기동 (새 PID)
- 결과: `/` **12.9 kB** / 209 kB First Load (AbortController 코드 +0.4 kB), 11 pages 전부 성공
- R1 기준 (15.8 kB) 대비 **−18%** 유지

### 누적 Session 17 최적화 요약
- R1: structures.find→Map, next.config 강화
- R2: page.tsx 조건부 컴포넌트 lazy-load
- R3: hit-test bbox early-reject, slice ±1 idle prefetch
- R4: StructurePanel 검색 haystack precompute
- R5: label fetch race AbortController

### Git Commits (Session 17 Round 5)
- (이 commit: label race AbortController + build verify)

## Session 17 — Round 6 (2026-04-22)
- Phase: perf round 6 (tail — module-level static split)

### 라이브 테스트
- 8 라우트 전부 200, 좀비 없음 (R5 재기동한 node 3-proc + python 2개 활성)
- `/tmp/bodyatlas-dev.log`에 에러 없음, 정상 compile+serve 흐름

### 성능 최적화 (R6)
- **`RegionSelector.tsx`:** `BODY_REGIONS.filter(r => r.group === 'original'|'spinai')`가 매 렌더마다 2회 호출되던 것을 모듈 스코프 `const ORIGINAL_REGIONS` / `const SPINAI_REGIONS`로 단 1회로 hoist. BODY_REGIONS는 immutable이라 안전.
  - activeRegion / locale 바뀔 때마다의 filter O(N) 중복 제거.

### 검증
- `npx tsc --noEmit` clean
- HMR 후 3 라우트 200
- `next build` 생략 (R2 교훈 — R5에서 이미 검증된 사이즈 유지)

### Git Commits (Session 17 Round 6)
- (이 commit: RegionSelector 모듈 스코프 filter hoist)

## Session 17 — Round 7 (2026-04-22)
- Phase: dep cleanup + security note

### 라이브 테스트
- 8 라우트 전부 200, dev 로그 clean, 좀비 없음
- **변화:** 이전 라운드까지 떠있던 학습 프로세스 (PID 74996, pseudo_xray34c_anon.py) 종료됨 — 완료/정리 여부는 D:/ImageLabelAPI_SPINAI 측에서 확인 필요 (auto_model_monitor 대상)

### dep 청소 (`package.json`)
- **미사용 5개 제거:** `@radix-ui/react-dialog`, `@radix-ui/react-slider`, `@radix-ui/react-tabs`, `@radix-ui/react-tooltip`, `@supabase/auth-helpers-nextjs`.
- 확인 방법: `grep -r "@radix-ui\|@supabase/auth-helpers-nextjs" src/` → 0 히트. package.json/lock에만 존재.
- 결과: `npm uninstall` 5개 + transitive 40개 = 총 45 packages 제거 (node_modules slim).
- 번들 크기 변화 없음 (tree-shaking으로 이미 미포함이었음) — `/` 12.9 kB / 209 kB First Load 유지.

### ⚠️ 보안 점검 (결정 보류 — 사용자 확인 필요)
- `npm audit`: next@14.2.35에 **1 high severity** (Image Optimizer DoS, RSC DoS, HTTP smuggling 등 5종 CVE 누적)
- fix: `next@16.2.4` (major upgrade, breaking change)
- **자동 실행 안 함** — major upgrade는 app layer 영향 크므로 사용자 승인 필요
- 권장: 별도 세션에서 `next@16` migration (App Router 변경점 검토 필수)

### 빌드 (safe mode)
- dev 전체 kill → `.next` 삭제 → `npx next build` → dev 재기동
- 결과: 11 pages 모두 성공, `/` 12.9 kB, First Load 209 kB (기준선 유지)

### Git Commits (Session 17 Round 7)
- (이 commit: 미사용 deps 5개 제거 + build 검증)

## Session 17 — Round 8 (2026-04-22)
- Phase: perf round 8 (list item memoization)

### 라이브 테스트
- 8 라우트 전부 200, dev 정상 compile
- 좀비 없음 — 학습 proc는 R7에 이미 종료, 현재 uvicorn(46648) + dev 3-proc만 활성

### 성능 최적화 (R8)
- **`StructurePanel.tsx` 리스트 아이템 memoization:**
  - 이전: `items.map(s => <button onClick={() => onStructureSelect(s)} />)` — 인라인 핸들러 + 부모 리렌더마다 275 버튼 전부 리렌더.
  - 지금: `StructureItem` 분리 + `React.memo` 래핑, `useCallback`으로 `stableSelect` 안정화. `selectedStructure` 변경 시 이전 선택 + 신규 선택 2개 아이템만 리렌더.
  - brain-mri 275 구조 기준: hover/click로 선택 변경할 때 리렌더 273 스킵 (99.3% 감소).
  - 부수 효과로 `getEnName` / `getLocalName`도 아이템 내부에서 1회씩 계산되어 React reconciliation 비용 감소.

### 검증
- `npx tsc --noEmit` clean
- HMR 후 2 라우트 200
- `next build`는 R7에서 이미 12.9 kB / 209 kB 검증 완료, 코드 형태 변경만이라 생략

### Git Commits (Session 17 Round 8)
- (이 commit: StructureItem memo 컴포넌트 분리)

## Session 17 — Round 9 (2026-04-22)
- Phase: perf round 9 (contour path memo) + build verify

### 라이브 테스트
- 6 라우트 전부 200, 좀비 없음 — uvicorn(46648) + dev 3-proc (재기동 후)

### 성능 최적화 (R9)
- **`AtlasViewer.tsx` SVG 경로 문자열 메모화:**
  - 이전: 각 contour마다 `contourPath(contour)` 호출로 `map(...).join('') + 'Z'` 문자열 매 렌더마다 재생성. hover로 `hoveredStructure` 변경 시 전체 JSX 재평가 → 모든 contour의 path string 재생성.
  - 지금: `labelIndex`에 `paths: l.contours.map(c => c.length<3 ? null : contourPath(c))` 추가. labels가 바뀔 때만 path 생성, hover 같은 빈번한 상태 변경엔 재계산 없음.
  - brain-mri 275 구조, 슬라이스당 평균 수십 contour 기준으로 string concat을 hover 프레임당 수백 건 → 0건.

### 빌드 (safe mode)
- dev kill → `.next` clean → `npx next build` → dev 재기동
- 결과: `/` **13 kB** / 209 kB First Load (R5/R7 12.9 → 13.0 kB, 정밀도 차이 수준)
- 11 pages 모두 성공

### 세션 17 누적 요약 (9 rounds)
- R1: structures.find→Map, next.config 강화
- R2: lazy-load 조건부 컴포넌트 (`/` 15.8→12.5 kB)
- R3: bbox early-reject, slice ±1 idle prefetch
- R4: 검색 haystack precompute
- R5: label fetch race AbortController
- R6: RegionSelector 모듈 스코프 filter
- R7: 미사용 deps 5개 제거 (45 packages prune)
- R8: StructurePanel item React.memo (brain-mri 99% 리렌더 감소)
- R9: SVG path string 메모화

### Git Commits (Session 17 Round 9)
- (이 commit: contourPath 메모화 + build 재검증)

## Session 17 — Round 10 (2026-04-22)
- Phase: perf round 10 (context value stabilization)

### 라이브 테스트
- 6 라우트 200, 좀비 없음, dev 3-proc (새 PID) + uvicorn 활성

### 성능 최적화 (R10)
- **Context value fresh-object 문제 수정:**
  - 이전: `I18nProvider` / `AuthProvider` 둘 다 `<Provider value={{...}}>`로 **매 렌더마다 새 객체** 생성. React는 Object.is 비교라 모든 `useI18n()` / `useAuth()` consumer가 provider 리렌더마다 강제 리렌더.
  - `I18nContext`: `t`를 `useCallback([locale])`, value를 `useMemo([locale, t])`로 stable.
  - `AuthContext`: `markTrialUsed` / `signInWith*` / `signOut` 5개 전부 `useCallback([])`, value를 `useMemo([user, loading, trialUsed, needsAuth, ...callbacks])`로 stable.
  - 실제 상태 변경 없는 provider 리렌더에서 consumer 리렌더 skip.
  - Header, FeedbackButton 등 `useI18n()` 사용처, 그리고 page.tsx의 `useAuth()` consumer들이 혜택.

### 검증
- `npx tsc --noEmit` clean
- HMR 후 2 라우트 200

### Git Commits (Session 17 Round 10)
- (이 commit: context value useMemo + useCallback)

## Session 17 — Round 11 (2026-04-22)
- Phase: dead state removal + build (큰 번들 감소)

### 라이브 테스트
- 6 라우트 200, 좀비 없음 (uvicorn + dev 3-proc 재기동)

### 발견한 dead state / dead ref
- **`AtlasViewer.tsx`**: `imgRef = useRef<HTMLImageElement>(null)` — `<img ref={imgRef}>`로 set되지만 **어디에서도 read 안 함**. 완전 dead ref.
- **`SpineXrayViewer.tsx`**: `[hoveredView, setHoveredView]` state — `setHoveredView(view)`가 **매 mouseMove마다** 호출되지만 어디에서도 read 안 함. mouseMove 프레임당 불필요한 setState → re-render 유발.

### 수정
- `imgRef` 선언 + `<img ref={imgRef}>` 제거.
- `hoveredView` state 선언 + `setHoveredView(...)` 2곳 전부 제거.

### 빌드 결과 (!)
- dev kill → `.next` clean → `npx next build`
- `/` 13 kB → **8.99 kB** (−31% 이번 라운드, R1 baseline 15.8 kB 대비 **−44%**)
- First Load JS 210 kB (shared 87.4 kB + page-specific 감소)
- 11 pages 모두 성공
- 이 정도 감소는 R10 context memo + R11 dead state 조합이 트리셰이킹에 영향을 준 듯 (NextJS가 더 aggressively prune)

### 세션 17 누적 번들 추이 (`/` 라우트)
- R0 baseline: 15.8 kB
- R2 (lazy-load): 12.5 kB (−21%)
- R5 (+AbortController): 12.9 kB
- R9 (+memoization): 13.0 kB
- R11 (dead state prune): **8.99 kB** (−44% from baseline)

### Git Commits (Session 17 Round 11)
- (이 commit: dead state/ref 제거 + build)

## Session 17 — Round 12 (2026-04-22)
- Phase: 마이너 cleanup (dead imports)

### 라이브 테스트
- 6 라우트 200, 좀비 없음

### Cleanup (R12)
- **`StructurePanel.tsx`**: `import { Search, Eye, EyeOff }`에서 `Eye`, `EyeOff` 미사용 확인 → 제거. 
- 전체 lucide-react import 스캔 결과 다른 컴포넌트는 모두 사용 중 (AuthGate, Header, FeedbackButton, InstallPrompt, ErrorBoundary, RegionSelector, not-found 전부 clean).
- 번들 영향: lucide-react는 개별 import 트리셰이킹이라 bundle-level 효과는 미미, 코드 hygiene 관점의 정리.

### 검증
- `npx tsc --noEmit` clean
- HMR 후 `/` 200
- `next build`는 R11에서 8.99 kB 검증되어 생략 (import만 줄이는 변경)

### Git Commits (Session 17 Round 12)
- (이 commit: Eye/EyeOff 미사용 import 제거)

## Session 17 — Round 13 (2026-04-22)
- Phase: correctness fix (setTimeout cleanup)

### 라이브 테스트
- 6 라우트 200, 좀비 없음 (uvicorn + 재기동 dev 3-proc)

### 버그 수정 (InstallPrompt)
- **`src/components/InstallPrompt.tsx`**: `beforeinstallprompt` handler 내부 `setTimeout(() => setShow(true), 30000)` 이 **useEffect cleanup에 포함 안 됨**. 컴포넌트가 이 30초 창 중에 unmount되면 timer가 살아남아 unmounted component setState 호출 가능 (React warning + 잠재 메모리 누수).
- 수정: effect 스코프에 `timerId` 변수로 추적, handler 재호출 시 이전 timer clear, cleanup에서도 `clearTimeout`.

### 빌드 (safe mode)
- dev kill → `.next` clean → `npx next build` → dev 재기동
- 결과: `/` **8.99 kB / 210 kB** (R11 수준 유지)
- 11 pages 모두 성공

### Git Commits (Session 17 Round 13)
- (이 commit: InstallPrompt setTimeout cleanup)

## Session 17 — Round 14 (2026-04-22)
- Phase: health check + SPINAI 상태 스냅샷 (코드 변경 없음)

### 라이브 테스트
- 6 라우트 200, 좀비 없음

### SPINAI 학습 상태 (user 메모 규칙에 따른 체크)
- `D:\ImageLabelAPI_SPINAI\outputs\models\unet_xray_ANON_v1_c34_20260421/` 디렉터리 존재, `best_model.pth` + `training_history.json` 있음 (R7에서 종료된 pseudo_xray34c 프로세스의 산출물).
- `training_history.json` 구조: `[{epoch, tr_loss, val_loss, ...}]` — epoch 0 기록 확인. 학습 완료 여부와 dice 추이는 `auto_model_monitor.py` 돌려서 dice threshold 통과 시 BodyAtlas 반영 결정 (사용자 몫).
- `D:\ImageLabelAPI_SPINAI\data\anon_seeds\xray_lumbar_ANON_v2_20260422/` 신규 seeds 디렉터리 확인 — 오늘 날짜 기준 pseudo-label 배치 출력물.

### Dead state scan (변경 없음)
- 모든 컴포넌트의 `useState` / `useRef` 선언과 사용처 grep 검증 → R11에 발견한 `imgRef` / `hoveredView` 외 추가 dead 없음.
- `AtlasViewer.tsx`는 10개 state + 1 ref 전부 legitimate.
- `SpineXrayViewer.tsx`는 R11 정리 후 8 state + 2 ref 전부 legitimate.
- `FeedbackButton.tsx`, `AuthGate.tsx`, `InstallPrompt.tsx`, `StructurePanel.tsx`도 clean.

### 변경 없음
- 코드 변경 없음 → commit 없음. 이 log만 append해도 의미 있는 feature 변동 아니라 pre-commit이 `[skip-features-check]` 없이 통과할지 애매 → 다음 변경과 함께 묶어 커밋하기로 결정.

## Session 17 — Round 15 (2026-04-22)
- Phase: perf round 15 (JSON-LD 모듈 스코프 + R14 dangling log commit 포함)

### 라이브 테스트
- 6 라우트 200, 좀비 없음 (dev 재기동 후)

### 성능 최적화 (R15)
- **`src/app/layout.tsx` JSON-LD 호이스팅:**
  - 이전: `jsonLd` 배열 (3 schemas, 약 145줄)이 `RootLayout` 컴포넌트 내부에서 매 서버 렌더마다 재구성 + `{jsonLd.map(JSON.stringify)}` 렌더 시 3번 재직렬화.
  - 지금: `const JSON_LD = [...]` + `const JSON_LD_STRINGS = JSON_LD.map(s => JSON.stringify(s))` 모듈 스코프로 hoist. 빌드 시 1회 직렬화, 모든 응답에서 byte-identical `<script>` 내용 재사용.
  - static 라우트에서는 ISR cache로 대체 완화되지만, 여전히 코드 경로상 깨끗해짐.

### 빌드 (safe mode)
- dev kill → `.next` clean → `npx next build` → dev 재기동
- 결과: `/` **8.99 kB / 210 kB** 안정 유지 (R11/R13와 동일)
- 11 pages 모두 성공

### Git Commits (Session 17 Round 15)
- (이 commit: JSON-LD 호이스팅 + R14 no-op log를 함께 commit)

## Session 17 — Round 16 (2026-04-22)
- Phase: 마이너 cleanup

### 라이브 테스트
- 6 라우트 200, 좀비 없음

### Cleanup
- `src/app/download/DownloadContent.tsx`: `import { ..., Wifi, WifiOff, ... }`에서 `Wifi` 사용처 0 확인 → 제거 (line 294에서 `WifiOff`만 사용).

### 검증
- `tsc --noEmit` clean, HMR 후 `/download` 200

### Git Commits (Session 17 Round 16)
- (이 commit: Wifi 미사용 import 제거)

## Session 17 — Round 17 (2026-04-22)
- Phase: JSON-LD 호이스팅 2차 (how-to-use)

### 라이브 테스트
- 6 라우트 200, dev 3-proc + uvicorn 활성, 좀비 없음

### 최적화 (R17)
- **`src/app/how-to-use/page.tsx`**: `faqJsonLd` / `howToJsonLd`이 컴포넌트 내부에서 선언 + `JSON.stringify(...)` 렌더 시마다 실행. `faqs`, `steps`는 이미 모듈 스코프 상수라 JSON-LD도 결정론적.
  - 모듈 스코프 `FAQ_JSON_LD` / `HOWTO_JSON_LD`로 hoist + **이미 직렬화된 문자열 저장** (한 번만 `JSON.stringify` 실행).
  - `dangerouslySetInnerHTML={{ __html: FAQ_JSON_LD }}` 형태로 즉시 주입.
  - R15의 layout.tsx JSON-LD 호이스팅과 동일 패턴.

### 검증
- `tsc --noEmit` clean, HMR 후 `/how-to-use` 200
- `next build` 생략 (R15에서 이미 검증된 8.99 kB 안정, layout 빌드 결과는 동일 레이어, how-to-use 영향은 서버 렌더 시간만)

### Git Commits (Session 17 Round 17)
- (이 commit: how-to-use JSON-LD 호이스팅)


---

## Session 18
- Date: 2026-04-22
- Round 18 (dynamic loop continuation)

### Startup State
- Dev 서버 다운 상태로 발견 (port 3000 EACCES).
- 원인: Windows `netsh interface ipv4 show excludedportrange protocol=tcp` 결과 포트 제외 범위에 2948-3147, 3233-3332 포함 → 3000/3001/3100 전부 차단. Hyper-V/WSL2 예약으로 추정.
- 회피: `PORT=3500` 으로 `npm run dev` 재기동 → 정상 listen.

### Live Test (port 3500)
- GET /           → 200 (0.084s)
- GET /how-to-use → 200 (0.037s)
- GET /download   → 200 (0.067s)
- GET /privacy    → 200 (0.070s)
- GET /terms      → 200 (0.081s)
- GET /compare/imaios → 404 (라우트 부존재 정상)
- 전 페이지 sub-100ms, 콘솔 에러/경고 0건.

### Zombie Cleanup
- conhost/cmd 11개 점검 중 오래된 10개 종료: 6108(cmd 04-11), 4620(04-16), 17288(04-02), 25012(04-02), 43124(04-11), 46592(04-10), 48640(04-20), 51144(04-18), 55760(04-18), 60172(04-18).

### Optimization Scan
- `grep TODO|FIXME|XXX|HACK` → 0건.
- `console.log/warn/error` 직접 사용 → logger.ts와 GlobalErrorListener 주석만 (정상).
- sitemap/robots/layout/page 추가 호이스팅 포인트 없음.
- R1~R17 이미 의미 있는 최적화 전부 소진. 본 라운드 코드 변경 0.

### Outcome
- 코드 커밋 없음 (기능/성능 변경 없음).
- 운영 복구 기록만 남김. 다음 세션에서 dev 재기동 시 `PORT=3500` 사용 권장.


## Session 19 (2026-04-22) — Round 19: data cache + decode hint + contourPath hoist
- Phase: perf round 19 (HTTP cache, image decode, latent TDZ fix, PWA sw cache)

### 라이브 테스트
- 시작 시점: 모든 node 프로세스(R18 dev 3500/3777 포함)가 죽어있는 상태로 발견.
  netstat은 LISTENING 표시했으나 connect 시 refused — Windows TCP 잔존만 있고 user-space는 죽음. 별도 zombie 5개 kill.
- `PORT=3500 npx next dev -p 3500` 새로 띄움 → 9 라우트 전부 200 (`/`, `/about`, `/how-to-use`, `/download`, `/privacy`, `/terms`, `/sitemap.xml`, `/robots.txt`, `/opengraph-image`).
- 9개 atlas dataPath `structures.json` + `info.json` 검증 시 `/data/our-brain-mri` 404 — 실제 dataPath는 `/data/brain-mri-commercial`로 RegionSelector에서 확인되어 false alarm. 9 atlas 전부 200.

### 발견한 버그/개선점
1. **`AtlasViewer.tsx`의 `contourPath` 함수 latent TDZ 위험:**
   - `useMemo([labels])` 콜백이 첫 렌더 시 동기 실행되는데 `contourPath`가 그 아래 라인에서 선언됨 → ReferenceError 위험.
   - 현재 첫 렌더 `labels=[]`라 우연히 안 터짐. SSR rehydrate 등 동기 경로 추가 시 즉시 크래시.
2. **메인 viewer `<img>`에 decode hint 없음** — LCP 후보인데 `decoding`/`fetchPriority` 미설정 → 메인스레드 디코드.
3. **`/data/*` 경로 long-cache 헤더 부재** — 슬라이스 PNG 평균 ~15 kB × 200~512 슬라이스 × 9 atlas = 수십 MB. URL `?v3` cache-busting 이미 있어 immutable 안전.
4. **PWA 서비스워커 캐시 antipattern** — `/sw.js`/`/manifest.json` 명시적 `Cache-Control` 부재.

### 수정
- `AtlasViewer.tsx`:
  - `contourPath` 모듈 스코프 hoist + `for` 루프로 단순화.
  - 메인 `<img>`에 `decoding="async"` + `fetchPriority="high"`.
- `next.config.js` `headers()` 신규:
  - `/data/:path*` → `Cache-Control: public, max-age=31536000, immutable`
  - `/sw.js` → `no-cache, no-store, must-revalidate` + `Content-Type: application/javascript; charset=utf-8`
  - `/manifest.json` → `public, max-age=3600`

### 빌드 (safe mode)
- dev 5개 kill → `.next` clean → `npx next build` → dev 재기동
- 결과: `/` **9.03 kB / 210 kB** First Load (R11 baseline 8.99 kB 대비 +0.04 kB — contourPath top-level 비용)
- 11 pages 모두 성공
- HTTP `Cache-Control: public, max-age=31536000, immutable` 헤더 `/data/our-ct/info.json` curl 직접 확인 ✓

### 검증
- `tsc --noEmit` clean
- 9 라우트 재테스트 200
- Cache-Control 헤더 curl 확인

### Git Commits (Session 19)
- (이 commit: contourPath hoist + img decode hint + /data immutable + sw.js no-cache + R18 log finalization)

### 배포 상태
- Vercel prod는 Session 15부터 다운 — 사용자 결정 대기 (이 라운드도 빌드 검증까지만, 푸시는 GitHub만).

## Session 20 (2026-04-22) — Round 20: drift correction + dead atlas 삭제
- Phase: discipline reset (per user iter rules)

### Drift 신호 점검 (사용자 규칙 적용)
- (1) 같은 모듈 영역 3+ iter 연속: AtlasViewer.tsx → R3, R5, R8, R9, R11, R13, R19. **HIT.**
- (2) primary metric 정체: `/` First Load JS R11~R19 8.99~9.03 kB, 4 iter 정체. **HIT.**
- (3) 3+ "나중에" 미뤄진 항목: Vercel(5 iter), quizMode(20 iter+ flag만), custom domain(8 iter). **HIT.**
- (4) 환경 블로커 2+ iter "재시도만": Vercel 404 status check 5 iter. **HIT.**
- 4개 신호 전부 firing → discipline reset 라운드.

### 공식 선언 (이 iter)
1. **공식 종료: 번들 사이즈 추적** — `/` First Load JS 9.03 kB가 floor. 3 iter 동일. 의미 있는 변동 없음. 다음 라운드부터 *번들 kB 변동 보고 중단*. tsc/build PASS만 verify.
2. **공식 포기: Vercel prod 자동 복구** — 자격증명·URL·도메인 결정 전부 사용자 영역. 5 iter "재시도만"으로 시그널 노이즈만 누적. 다음 라운드부터 *prod URL 헬스 체크 중단*. 사용자가 deploy 액션 취하면 그때 다시 다룸. (HANDOVER에 별도 기록 권장.)
3. **이번 iter primary outcome (20자 이내)**: `Dead atlas 데이터 삭제` (12자)

### 산출물 (rule 3 — 삭제/단순화 + 핵심 숫자 이동)
1. **`public/data/brain-pet/` 삭제** (3.3 MB)
   - FEATURES.md에서 "(미사용)" 표기, RegionSelector 미등록, 코드 0 참조 → 진짜 dead.
2. **`public/data/spine-xray/` 삭제** (49 KB)
   - FEATURES.md에서 "(레거시, 미연결)", RegionSelector 미등록, `SpineXrayViewer.tsx`의 default prop만 참조.
3. **`SpineXrayViewer.tsx` default `dataPath`**: `/data/spine-xray` → `/data/our-xray`로 교체 (dangling reference 제거).
4. **`FEATURES.md` 갱신**: brain-pet, spine-xray 행 삭제.

### Impact (핵심 숫자)
- `public/data/`: 284 MB → **281 MB** (−3.35 MB, −1.18%)
- Vercel deploy bundle: 동일 -3.35 MB
- 9개 등록 atlas (head-ct, chest-ct, our-head-ct, our-ct, brain-mri-commercial, our-lumbar-mri, our-xray, our-hand-xray, our-foot-xray) 영향 0 — 라이브 200 검증.

### 검증
- `tsc --noEmit` clean
- `next build` PASS — 11 pages, `/` 9.03 kB / 210 kB (이번 iter는 data 삭제만, code 변경은 default prop 1줄 → 번들 영향 0)
- `next dev` 재기동 → 9 라우트 200, 9 atlas 200, 삭제된 2 atlas 404 (의도대로)

### Next iter 가이드
- 같은 perf 폴리싱 영역 회피 (AtlasViewer.tsx, layout.tsx, page.tsx의 micro-opt 금지)
- 측정 대상 변경 후보: atlas structure 수, 사용자 가치 metric, content quality, accessibility score
- 자유 실험 가능 영역: quizMode flag 실제 구현, 다국어 cover율, atlas 추가/품질, README/about 콘텐츠

### Git Commits (Session 20)
- (이 commit: dead atlas 삭제 + SpineXrayViewer dangling fix + FEATURES 갱신 + drift discipline 로그)


## Session 18 — Round 20
### Optimization Found
- **보안 헤더 전무**: `curl -I /` 결과 X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Permissions-Policy 전부 부재. Mozilla Observatory fail 수준. 의료/교육 콘텐츠 서비스로 신뢰성 흠.
- Fix: `next.config.js` `headers()`에 `/:path*` 베이스라인 추가:
  - `X-Content-Type-Options: nosniff` (MIME sniffing 차단)
  - `X-Frame-Options: SAMEORIGIN` (기본 clickjacking 보호)
  - `Referrer-Policy: strict-origin-when-cross-origin` (cross-origin referrer 최소화)
  - `Permissions-Policy: camera=(), microphone=(), geolocation=()` (미사용 기능 명시적 비활성)
- CSP는 의도적 제외 — AdSense/analytics script origin 전수 감사 없이 적용 시 광고/추적 전부 깨짐. 별도 라운드 필요.

### Verification
- dev 재기동 후 `curl -I /`: 4개 헤더 전부 적용 ✓.
- `/sw.js`: Cache-Control(no-cache) + Content-Type(JS) + X-Content-Type-Options + X-Frame-Options 스택됨 ✓.
- Route sweep: /, /how-to-use, /download, /privacy, /terms, /about 전부 200, sub-130ms.
- 좀비 프로세스: 6시간 이상 된 것 0개 (R18에서 모두 정리됨).

### Outcome
- 본 라운드는 short variant(커밋/푸시 미지정) → 커밋만 스테이징하지 않고 diff 상태로 둠.
- 다음 long variant 라운드에서 함께 커밋 예정.


## Session 18 — Round 21
### Live Test
- Route sweep: /, /how-to-use, /download, /privacy, /terms, /about 전부 200 (0.1~1.2s, dev HMR 재컴파일 영향).
- 좀비 프로세스 6시간 이상 된 것 0개.

### Optimization Found
- **robots.ts SEO 누출 버그**: 기본 `*` 규칙에는 `disallow: ['/api/', '/data/']`가 있으나, 그 뒤에 나열된 Googlebot/Bingbot/Naver/Baidu/Yandex 등 14개 봇 블록은 `allow: '/'`만 선언 → robots.txt spec상 named User-Agent 블록이 `*` 규칙을 OVERRIDE하므로, 주요 크롤러들이 `/data/` (의료 이미지 원본 JSON) 를 크롤할 수 있는 상태.
- Fix: 14개 중복 블록 제거, `*` 규칙 하나로 통합.
  - robots.txt 바이트 크기 ~600B → ~120B 감소 (부수 효과).
  - 모든 크롤러가 동일한 disallow 경로를 준수.
- R20에서 staged한 보안 헤더 4종(`X-Content-Type-Options` 등) 포함.

### Verification
- `curl /robots.txt` 결과 단일 규칙: `User-Agent: *` / `Allow: /` / `Disallow: /api/` / `Disallow: /data/` ✓.
- `tsc --noEmit` clean.

### Commit Plan
- 파일: `next.config.js` (보안 헤더 4종), `src/app/robots.ts` (중복 제거), `claude-progress.txt`.
- 메시지: `fix(seo,security): robots.txt data leak fix + baseline security headers [skip-features-check]`
- 빌드: dev 중지 → .next 삭제 → next build → dev 재기동.


## Session 18 — Round 22
### Live Test
- Route sweep: /, /how-to-use, /download, /privacy, /terms, /about 전부 200 (45~150ms).
- 좀비 프로세스 6시간 이상 된 것 0개.
- 개발 로그 에러/경고 0건.

### Observation
- 다른 세션(또는 사용자)이 동시 작업 중: `src/components/QuizPanel.tsx` 신규 파일, `src/app/page.tsx` + `src/lib/i18n.ts` 수정 상태 → 같은 파일에 중복 편집 위험 있어 본 라운드는 코드 변경 생략.
- 발견한 잠재 개선점 (우선순위 낮음, 다음 세션 위임):
  - `ServiceWorkerRegister`는 주기적 `reg.update()` 호출 없음 → 오래 열린 탭은 SW 업데이트 늦게 받음.
  - `sw.js` cache-first 리스트에 `/_next/data/` 및 `/data/` 경로가 빠져 있음 → Next.config에서 31536000 immutable 설정된 데이터가 런타임 SW 캐시 제외.
- 두 건 모두 적용 가능성은 있으나 QuizPanel 작업과 충돌 가능성이 있어 현재 세션에서는 보류.

### Outcome
- 커밋 없음 (short variant + 충돌 회피).


## Session 21 (2026-04-22) — Round 21: Quiz Mode MVP (high-risk feature 실험)
- Phase: drift correction + 신규 기능 실험 (per /loop discipline rules)

### Drift 신호 점검
- (1) 같은 모듈 영역 3+ iter 연속 — R20에서 이미 pivot, 본 라운드는 새 컴포넌트 + page.tsx + i18n. **N/A.**
- (2) primary metric 정체 — bundle 추적 R20에서 공식 종료. **N/A.**
- (3) 3+ "나중에" 미뤄진 항목 — `quizMode` flag (Session 1부터 20+ iter). **HIT → 본 라운드에서 실구현.**
- (4) 환경 블로커 — Vercel R20에서 공식 포기. **N/A.**

### Primary outcome (10자)
`퀴즈 모드 MVP 구현` — rule 3(b) 실패 확률 ≥50% 실험 (UX uncertainty + 기존 viewer click 흐름 통합 위험).

### 산출물
- **`src/components/QuizPanel.tsx` 신규** (175줄):
  - 현재 atlas의 `structures.json` 로드 (AbortController로 race 방지)
  - 랜덤 구조 1개 추첨 → 로컬 displayName (`displayName[locale] || displayName.en || name`)으로 출제
  - viewer 클릭 → `selectedStructure.id === target.id` 채점, `lastJudgedIdRef`로 페어당 1회 채점
  - 정답/오답 배너 (Check / X 아이콘 + emerald/rose 컬러)
  - 점수 표시 + 정확도 % (correct/total + Math.round)
  - "Next question" 버튼 (직전 target 제외 랜덤 재추첨)
  - "Reset" 버튼 (점수 0 초기화)
  - dataPath 변경 시 자동 reset
- **`src/app/page.tsx` 수정**:
  - `quizMode: boolean` 신규 state
  - 모드 토글 pill (Search / Brain 아이콘) — RegionSelector 아래
  - 우측 패널 conditional: `quizMode ? <QuizPanel> : <StructurePanel>` (desktop + mobile 양쪽)
  - `QuizPanel` `next/dynamic` lazy-load (ssr:false) — 비활성 시 번들 미포함
- **`src/lib/i18n.ts`**: 13개 신규 키 (`mode.explore`, `mode.quiz`, `quiz.*`) EN+KO 정의. 5개 locale은 `t()`의 EN fallback 사용.
- **`FEATURES.md`**:
  - §1.1 모드 토글 + 우측 패널 분기 설명 추가
  - §2.4b QuizPanel 신설 (sub-features, i18n keys, activation)

### 검증
- `tsc --noEmit` clean (zero error)
- `next build` PASS — 11 pages, `/` 9.95 kB / 211 kB (R19 9.03 → +0.92 kB; QuizPanel 추가 비용. 번들 추적 종료 선언했으나 참고치).
- dev 재기동 → 9 라우트 200
- HTML 렌더 검증: `curl /` 응답에 "Explore" + "Quiz" 토글 텍스트 노출 확인 ✓
- 좀비 정리: dev 재기동 전 잔존 node 4개 kill (R19/R20 빌드 잔존)

### 실험 위험 (≥50% 평가)
- 기존 viewer click 흐름과 통합 — `selectedStructure` 변경이 AtlasViewer의 bestSlice 자동 점프 트리거. quiz 답 클릭 후 jump가 일어나는데, 이는 답 위치 시각 확인 효과 (positive) vs 클릭 위치에서 멀어짐 (UX 혼동). MVP는 그대로 두고 사용자 feedback 후 결정.
- mobile UX — desktop과 동일 토글 + panel placement, 별도 터치 최적화 안 함. 향후 검토.
- 라벨 overlay ON 상태에서 quiz는 컬러로 답이 보임 → "쉬움 모드". 향후 difficulty 토글로 hide-labels challenge 추가 가능.

### Negative result 가능성
- 사용자가 quiz를 안 쓰면 lazy-load 비용은 0 (dynamic import). 본 실험의 downside risk는 dead code 추가 + 유지보수 부담뿐.
- 만약 사용자가 "쓸데없다"고 판단하면 next round에서 제거 + flag 정리 (negative result도 지식, log).

### Git Commits (Session 21)
- (이 commit: QuizPanel + page.tsx 모드 토글 + i18n 13 keys + FEATURES 갱신)

### 배포 상태
- R20 declaration대로 prod 헬스 체크 안 함. GitHub push까지만.


## Session 18 — Round 23
### Context
- 다른 세션에서 Quiz Mode MVP 커밋 (`8cef09a0`) 완료 → 워크트리 클린, 안전하게 내 수정 가능 상태.
- 라우트 6개 전부 200 sub-80ms, 좀비 0.

### Optimization Applied
- `ServiceWorkerRegister`: `setInterval(() => reg.update(), 60*60*1000)` 추가 + cleanup.
  - 근거: 기존 코드는 탭 로드 시 1회만 SW 등록/업데이트 체크. PWA 설치 후 학습 세션을 몇 시간씩 여는 사용자(의대생 시험 대비)는 새 SW 버전을 바로 못 받음.
  - `reg.update()`는 sw.js가 안 바뀌었으면 HTTP 304 → 거의 무비용.

### Not Applied (R22 후보 재검토)
- `sw.js` cache-first 리스트에 `/data/` 추가 건: 기각.
  - 이유: `/data/*.json`은 region당 수십~수백 MB. 한 슬라이스만 봐도 SW가 전체 region을 IndexedDB에 캐싱하게 되어 스토리지 쿼터 초과 위험. 그리고 next.config의 `max-age=31536000 immutable`이 이미 HTTP 캐시로 동일한 오프라인-가까운 동작을 저비용으로 제공. SW 캐싱은 중복이자 역효과.
  - `/_next/data/` 추가도 보류: 이 앱은 거의 전부 정적 생성이라 실질 페이로드가 거의 없음.

### Verification
- `tsc --noEmit` clean.
- `git status` clean (아래 커밋 후).

### Commit Plan
- 파일: `src/components/ServiceWorkerRegister.tsx`, `claude-progress.txt`.
- 메시지: `feat(pwa): periodic reg.update() for long-lived tabs [skip-features-check]`
- 푸시 후 빌드: stop dev → rm .next → next build → restart dev.


## Session 18 — Round 24
### Live Test
- Route sweep: 6개 전부 200 (200~330ms, HMR 유휴 상태 정상 범위).
- 좀비 프로세스 6시간 이상: 0.
- 개발 로그 에러/예외: 0건.

### Concurrent Session
- 다른 세션 활동: `src/config/features.ts` 삭제, `src/lib/i18n.ts` 수정. 해당 파일에 간섭 금지.

### Outcome
- 신규 이슈 없음. 코드 변경 없음. 커밋 없음.


## Session 22 (2026-04-22) — Round 22: Dead config 제거 + Quiz i18n parity
- Phase: drift-aware iter, two orthogonal wins

### Drift 신호 점검
- 4 신호 모두 firing 안 함 (R20/R21에서 perf/Vercel 이미 정리, Quiz 어제 출시).
- **그러나 발견**: `src/config/features.ts` 9-flag 시스템 + `isFeatureEnabled()` + `FeatureKey` 타입 → 코드 베이스 0 참조 dead code. 새 contributor가 "퀴즈는 quizMode flag로 켠다"고 misread할 confusion source.

### Primary outcome (12자)
`Dead feature config 제거` — rule 3(c) 삭제/단순화.
보너스: Quiz i18n 5 locale 번역 — rule 3(a) 핵심 숫자 이동 (1 → 7 locale coverage).

### 산출물
1. **`src/config/features.ts` 삭제** + 빈 `src/config/` 디렉터리 제거.
   - 검증: `grep -rn "from '@/config/features'\|isFeatureEnabled\|FeatureKey" src/ scripts/` → 0 hits.
   - 영향: 빌드/런타임 영향 0 (어차피 import 안 되던 dead code), 코드 베이스 명확성 ↑.

2. **`src/lib/i18n.ts`** Quiz 키 5 locale (ja/zh/es/de/fr) 추가:
   - 13 keys × 5 locales = **65 신규 번역**.
   - Quiz UI locale coverage: EN+KO (29%) → 7 locales (100%).

### 검증
- `tsc --noEmit` clean
- `next build` PASS — 11 pages, `/` 9.95 kB / 211 kB (R21과 동일, 번들 영향 0)
- dev 재기동 → 9 라우트 200
- 좀비 정리: dev 재기동 전 잔존 node 2개 kill

### 핵심 숫자 이동
- 지원 locale × Quiz UI 번역 완료율: **2/7 → 7/7 (100%)**
- src/config/ 디렉터리 제거 → 1개 dead module 정리.

### Vercel/번들
- R20 declaration 준수: prod 헬스 체크 skip, 번들 kB 변동 추적 skip.

### Next iter 후보 (drift 회피)
- ~~Quiz polish~~ (R21 출시, R22 i18n parity — 3 iter 회피 위해 다음 라운드는 다른 영역)
- 후보: AtlasViewer hard-mode (overlay 토글 prop), atlas content 추가, accessibility audit, README polish

### Git Commits (Session 22)
- (이 commit: features.ts 삭제 + Quiz i18n 5 locale + log)


## Session 18 — Round 25
### Live Test
- Route sweep: 6개 전부 200 (38~230ms).
- 좀비 6시간+ 0개. dev 에러 0건.

### Optimization Found
- **i18n 키 누락 버그**: `Header.tsx` 데스크톱/모바일 네비게이션의 `Guide` 링크가 하드코딩된 영문이었음. `header.about` + `header.download`는 7개 locale 전부 번역 되어 있는데 `header.guide`만 빠져 있어 한국어/일본어/중국어 사용자는 "Guide"가 영문으로 표시됨.
- Fix:
  - `src/lib/i18n.ts`: 7개 locale에 `header.guide` 추가.
    - en: `Guide`
    - ko: `가이드`
    - ja: `ガイド`
    - zh: `指南`
    - es: `Guía`
    - de: `Anleitung`
    - fr: `Guide`
  - `src/components/Header.tsx`: 두 군데 (desktop link + mobile menu link) `Guide` → `{t('header.guide')}`.

### Verification
- `tsc --noEmit` clean.
- `curl /` → `>Guide<` 렌더됨 (SSR 기본 locale=en 정상, client에서 사용자 locale로 swap 예정).

### Commit Plan
- 메시지: `fix(i18n): header.guide key for all 7 locales [skip-features-check]`
- 빌드: stop dev → rm .next → next build → restart dev.


## Session 18 — Round 26
### Live Test
- Route sweep: 6개 전부 200 (34~151ms).
- 좀비 6시간+ 0개. dev 에러 0건.

### Observation
- `public/sw.js` worktree 수정 상태 (내 R23에서 보류했던 `/data/` cache-first 추가). 작성자(사용자 또는 타 세션):
  - `CACHE_NAME: 'bodyatlas-v1' → 'bodyatlas-v2'` 으로 invalidation 기법 적용 → R23의 "업데이트 시 구 데이터 갇힘" 우려 해결.
  - 코멘트 명시: "offline mode works for structure panel + labels overlay".
- JS 문법 검증: `node -c public/sw.js` OK.
- 커밋 안 함 — 다른 작성자의 in-progress 변경으로 판단, 간섭 회피.

### Outcome
- 코드 변경 없음, 커밋 없음.


## Session 18 — Round 27
### Live Test
- Route sweep 6개 200 (62~267ms). 좀비 0. dev 에러 0.
- 다른 세션 sw.js 변경은 `078b16cf`로 커밋 완료 → 워크트리 클린.

### Optimization Found
- **Footer 불필요한 `'use client'`**: `src/components/Footer.tsx` 가 `'use client'` 지시자 사용 중이지만 상태/이벤트 핸들러/브라우저 API 전무 (Link + anchor + `new Date().getFullYear()`만). 클라이언트 경계가 무의미하게 생성되어 번들 크기에 영향.
- Fix: `'use client'` 제거 → 순수 서버 컴포넌트화. `getFullYear()`는 렌더 타임에 서버에서 실행되므로 정상 동작.
- 잔존 개선 후보 (본 라운드 보류): Footer 링크 텍스트("About", "How to Use", "Privacy Policy", "Terms of Service", "Contact")가 i18n 미적용 영문 하드코딩. 서버 컴포넌트화하면 `useI18n()` 사용 불가 → i18n 원한다면 `'use client'` 복구 필요. 현재 legal 링크 특성상 영문 유지가 실용적이라 변경 안 함.

### Verification
- `tsc --noEmit` clean.
- `curl /` 200 193ms, Footer 영역 정상 렌더링 (개발 HMR 통해 확인).

### Commit Plan
- 파일: `src/components/Footer.tsx`, `claude-progress.txt`.
- 메시지: `perf(footer): drop 'use client' — pure server component [skip-features-check]`
- 빌드: stop dev → rm .next → next build → restart dev.


## Session 18 — Round 28
### Live Test
- Route sweep 6개 200 (228~913ms, 다른 세션 HMR 재컴파일 영향으로 /about 유독 느림).
- 좀비 6시간+ 0개. dev 에러 0건.

### Concurrent Session Activity
- `src/app/page.tsx`, `src/components/AtlasViewer.tsx`, `src/components/QuizPanel.tsx` 3개 수정 상태.
- 시스템 리마인더로 `src/lib/i18n.ts`에 `quiz.easy` / `quiz.hard` 키 추가된 것 확인 (퀴즈 난이도 토글 기능 작업 중으로 추정).
- 모든 파일 간섭 금지.

### Outcome
- 신규 이슈 없음, 코드 변경 없음, 커밋 없음.


## Session 18 — Round 29
### Live Test
- Route sweep 6개 200 (32~201ms), 좀비 0, dev 에러 0.
- 다른 세션 커밋: `338fd84a feat(quiz): Hard mode — labels hidden` → 워크트리 클린.

### Optimization Found
- **문서-실제 동작 불일치**: `src/lib/logger.ts` `loggedFetch` JSDoc에 "Throws on non-2xx so callers can let errors propagate"라고 써있지만 실제 코드는 non-2xx 시 `log.fetchError` 호출 후 Response를 그대로 반환 (throw 안 함). 4개 호출자 (`AtlasViewer` 2곳, `SpineXrayViewer` 2곳) 전부 이미 `if (!res.ok) return;` 으로 방어 → 코드 동작이 올바르고 doc만 틀림.
- Fix: docstring을 실제 동작에 맞춰 수정. 네트워크 에러는 throw, HTTP 에러는 로그 후 res 반환 명시. 향후 컨트리뷰터가 잘못 이해해서 throw에 의존하는 코드를 쓰는 걸 방지.

### Not Applied
- `RegionSelector` 모든 region `free: true`라 `locked`/`Lock icon`/`isAuthenticated` prop이 dead code. 제거하려면 `page.tsx` 수정 필요한데 다른 세션이 작업 중이라 보류.
- `AuthGate` 영문 하드코딩 10개 문자열. 7개 locale 번역 → 70개 키. 단일 라운드 범위 초과.

### Commit Plan
- 파일: `src/lib/logger.ts`, `claude-progress.txt`.
- 메시지: `docs(logger): loggedFetch doc — non-2xx returns, doesn't throw [skip-features-check]`
- 빌드: stop dev → rm .next → next build → restart dev.


## Session 18 — Round 30
### Live Test
- Route sweep 6개 200 (42~66ms). 좀비 0. dev 에러 0.
- 다른 세션 a11y 작업 진행 중: `page.tsx`, `Footer.tsx`, `Header.tsx`, `i18n-context.tsx` 수정 상태. 미커밋.

### Outcome
- 신규 이슈 없음. 충돌 회피 위해 코드 변경/커밋 없음.
